#include "main.h"

/**
 * _islower -> checks if the character is lowercase
 * @c is a character
 * Return: return 1 and 0 depending on condition
 */
int _islower(int c)
{
	return ( c >= 'a' && c <= 'z' );
}
